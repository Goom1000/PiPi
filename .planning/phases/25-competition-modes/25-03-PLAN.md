---
phase: 25-competition-modes
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - components/games/shared/ScoreOverlay.tsx
  - components/games/shared/ScoreDisplay.tsx
autonomous: true

must_haves:
  truths:
    - "Score display shows player name in individual mode"
    - "Score display shows team names and scores in team mode"
    - "Active team is visually highlighted with glow effect"
    - "Teacher can manually adjust scores with +/- buttons"
  artifacts:
    - path: "components/games/shared/ScoreOverlay.tsx"
      provides: "Teacher-side score display with manual adjustment"
      exports: ["default"]
      min_lines: 50
    - path: "components/games/shared/ScoreDisplay.tsx"
      provides: "Student-side score display (read-only)"
      exports: ["default"]
      min_lines: 40
  key_links:
    - from: "components/games/shared/ScoreOverlay.tsx"
      to: "types.ts"
      via: "import CompetitionMode"
      pattern: "import.*CompetitionMode.*from"
    - from: "components/games/shared/ScoreDisplay.tsx"
      to: "types.ts"
      via: "import CompetitionMode"
      pattern: "import.*CompetitionMode.*from"
---

<objective>
Create score display components for teacher and student views.

Purpose: Show competition progress during gameplay. Teacher view includes manual score adjustment buttons; student view is read-only with visual emphasis on active team/player.

Output: ScoreOverlay.tsx (teacher) and ScoreDisplay.tsx (student) components
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-competition-modes/25-CONTEXT.md
@.planning/phases/25-competition-modes/25-RESEARCH.md
@types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScoreOverlay component (teacher view)</name>
  <files>components/games/shared/ScoreOverlay.tsx</files>
  <action>
Create `components/games/shared/ScoreOverlay.tsx`:

```typescript
import React from 'react';
import { CompetitionMode, Team } from '../../../types';

interface ScoreOverlayProps {
  competitionMode: CompetitionMode;
  onUpdateScore?: (teamIndex: number, delta: number) => void;
}

/**
 * Teacher-side score display with manual adjustment buttons.
 * Positioned in top-right corner during gameplay.
 * Shows player name (individual) or team scores with +/- controls (team).
 */
const ScoreOverlay: React.FC<ScoreOverlayProps> = ({
  competitionMode,
  onUpdateScore
}) => {
  if (competitionMode.mode === 'individual') {
    // Individual mode - just show player name badge
    const displayName = competitionMode.playerName || 'Player';

    return (
      <div className="fixed top-4 right-4 z-40">
        <div className="px-4 py-2 rounded-xl bg-slate-900/80 backdrop-blur-sm border-2 border-amber-400 shadow-lg">
          <div className="text-xs text-slate-400 uppercase tracking-wider">Playing</div>
          <div className="text-lg font-bold text-white">{displayName}</div>
        </div>
      </div>
    );
  }

  // Team mode - show all teams with scores and adjustment buttons
  const { teams, activeTeamIndex } = competitionMode;

  return (
    <div className="fixed top-4 right-4 z-40 flex flex-wrap gap-2 max-w-md">
      {teams.map((team, index) => {
        const isActive = index === activeTeamIndex;

        return (
          <div
            key={team.id}
            className={`
              px-3 py-2 rounded-xl bg-slate-900/80 backdrop-blur-sm
              border-2 transition-all duration-300
              ${isActive
                ? 'border-amber-400 ring-2 ring-amber-400/50 scale-105 shadow-lg shadow-amber-400/30'
                : 'border-slate-600'
              }
            `}
          >
            {/* Team name */}
            <div className={`text-[10px] uppercase tracking-wider mb-1 ${
              isActive ? 'text-amber-400' : 'text-slate-400'
            }`}>
              {team.name}
            </div>

            {/* Score with +/- buttons */}
            <div className="flex items-center gap-1">
              {onUpdateScore && (
                <button
                  onClick={() => onUpdateScore(index, -1)}
                  className="w-5 h-5 rounded bg-slate-700 hover:bg-red-600 text-white text-xs font-bold transition-colors"
                  title="Decrease score"
                >
                  -
                </button>
              )}
              <span className="text-xl font-bold text-white min-w-[2ch] text-center">
                {team.score}
              </span>
              {onUpdateScore && (
                <button
                  onClick={() => onUpdateScore(index, 1)}
                  className="w-5 h-5 rounded bg-slate-700 hover:bg-green-600 text-white text-xs font-bold transition-colors"
                  title="Increase score"
                >
                  +
                </button>
              )}
            </div>

            {/* Active indicator */}
            {isActive && (
              <div className="text-[8px] text-amber-400 text-center mt-1 animate-pulse">
                PLAYING
              </div>
            )}
          </div>
        );
      })}
    </div>
  );
};

export default ScoreOverlay;
```

Key decisions per CONTEXT.md:
- Fixed position top-right (floating overlay)
- Individual mode: simple name badge
- Team mode: all teams visible with scores
- Active team: amber glow ring + scale + "PLAYING" indicator
- Manual +/- buttons for score correction
- Compact design to not obscure game content
  </action>
  <verify>
File exists: `ls components/games/shared/ScoreOverlay.tsx`
TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
ScoreOverlay component shows competition progress with manual adjustment controls for teacher
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ScoreDisplay component (student view)</name>
  <files>components/games/shared/ScoreDisplay.tsx</files>
  <action>
Create `components/games/shared/ScoreDisplay.tsx`:

```typescript
import React from 'react';
import { CompetitionMode } from '../../../types';

interface ScoreDisplayProps {
  competitionMode: CompetitionMode;
}

/**
 * Student-side score display (read-only).
 * Positioned in top-right corner during gameplay.
 * Shows player name (individual) or team scores (team) with active team highlighted.
 */
const ScoreDisplay: React.FC<ScoreDisplayProps> = ({ competitionMode }) => {
  if (competitionMode.mode === 'individual') {
    // Individual mode - show player name badge
    const displayName = competitionMode.playerName || 'Player';

    return (
      <div className="fixed top-4 right-4 z-40">
        <div className="px-6 py-3 rounded-2xl bg-slate-900/80 backdrop-blur-sm border-2 border-amber-400 shadow-lg">
          <div className="text-xs text-slate-400 uppercase tracking-wider text-center">Playing</div>
          <div className="text-2xl font-bold text-white text-center">{displayName}</div>
        </div>
      </div>
    );
  }

  // Team mode - show all teams with scores
  const { teams, activeTeamIndex } = competitionMode;

  return (
    <div className="fixed top-4 right-4 z-40 flex flex-wrap gap-3 max-w-lg">
      {teams.map((team, index) => {
        const isActive = index === activeTeamIndex;

        return (
          <div
            key={team.id}
            className={`
              px-4 py-2 rounded-xl bg-slate-900/80 backdrop-blur-sm
              border-2 transition-all duration-300
              ${isActive
                ? 'border-amber-400 ring-4 ring-amber-400/50 scale-110 shadow-lg shadow-amber-400/30'
                : 'border-slate-600'}
            `}
          >
            {/* Team name */}
            <div className={`text-[10px] uppercase tracking-wider ${
              isActive ? 'text-amber-400' : 'text-slate-400'
            }`}>
              {team.name}
            </div>

            {/* Score */}
            <div className="text-2xl font-bold text-white">{team.score}</div>
          </div>
        );
      })}
    </div>
  );
};

export default ScoreDisplay;
```

Key decisions:
- Larger font sizes than teacher view (visible from back of classroom)
- No adjustment buttons (read-only for students)
- More prominent active team indicator (ring-4 vs ring-2, scale-110 vs scale-105)
- Same positioning pattern as teacher view for consistency
  </action>
  <verify>
File exists: `ls components/games/shared/ScoreDisplay.tsx`
TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
ScoreDisplay component shows competition progress as read-only overlay for student view
  </done>
</task>

</tasks>

<verification>
```bash
# Files exist
ls components/games/shared/ScoreOverlay.tsx
ls components/games/shared/ScoreDisplay.tsx

# TypeScript compiles
npx tsc --noEmit

# Correct imports
grep "import.*CompetitionMode" components/games/shared/ScoreOverlay.tsx
grep "import.*CompetitionMode" components/games/shared/ScoreDisplay.tsx
```
</verification>

<success_criteria>
- [ ] ScoreOverlay shows player name badge in individual mode
- [ ] ScoreOverlay shows team scores with +/- buttons in team mode
- [ ] ScoreOverlay highlights active team with amber glow
- [ ] ScoreDisplay shows player name badge in individual mode (larger)
- [ ] ScoreDisplay shows team scores in team mode (larger, no buttons)
- [ ] ScoreDisplay highlights active team with more prominent glow
- [ ] Both components positioned fixed top-right
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-competition-modes/25-03-SUMMARY.md`
</output>
