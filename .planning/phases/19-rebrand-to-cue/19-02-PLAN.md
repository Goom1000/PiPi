---
phase: 19-rebrand-to-cue
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - vite.config.ts
autonomous: false

must_haves:
  truths:
    - "App is accessible at new GitHub Pages URL"
    - "All assets load correctly (no 404s)"
    - "Old repository URL redirects for git operations"
  artifacts:
    - path: "vite.config.ts"
      provides: "Build configuration with updated base path"
      contains: "base: '/"
  key_links:
    - from: "vite.config.ts"
      to: "GitHub Pages"
      via: "base path configuration"
      pattern: "base:"
---

<objective>
Complete the rebrand by renaming the GitHub repository and updating the deployment configuration.

Purpose: Make the application accessible at a professional URL that reflects the "Cue" branding.

Output: Application deployed and accessible at new GitHub Pages URL with all assets loading correctly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-rebrand-to-cue/19-RESEARCH.md
@.planning/phases/19-rebrand-to-cue/19-01-SUMMARY.md
@vite.config.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Choose new repository name</decision>
  <context>
The GitHub repository needs to be renamed from "PiPi" to reflect the "Cue" branding. This affects:
- GitHub Pages URL: `username.github.io/<repo-name>/`
- Vite base path configuration
- Any bookmarks or links to the current URL

The repository is currently at: https://github.com/goom1000/PiPi
Current URL: https://goom1000.github.io/PiPi/
  </context>
  <options>
    <option id="cue">
      <name>cue</name>
      <pros>Simple, matches brand name exactly, short URL</pros>
      <cons>Very generic name, may conflict with other projects</cons>
      <url>goom1000.github.io/cue/</url>
    </option>
    <option id="cue-app">
      <name>cue-app</name>
      <pros>Clear it's an application, follows GitHub conventions (lowercase-with-dashes)</pros>
      <cons>Slightly longer URL</cons>
      <url>goom1000.github.io/cue-app/</url>
    </option>
    <option id="cue-presentation">
      <name>cue-presentation</name>
      <pros>Descriptive, indicates purpose</pros>
      <cons>Long URL, verbose</cons>
      <url>goom1000.github.io/cue-presentation/</url>
    </option>
    <option id="cue-teacher">
      <name>cue-teacher</name>
      <pros>Indicates target audience</pros>
      <cons>May be too specific</cons>
      <url>goom1000.github.io/cue-teacher/</url>
    </option>
  </options>
  <resume-signal>Select: cue, cue-app, cue-presentation, or cue-teacher (or provide custom name)</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Rename GitHub repository</action>
  <reason>Repository rename requires GitHub account access and cannot be automated via CLI without admin permissions.</reason>
  <instructions>
1. Go to: https://github.com/goom1000/PiPi/settings
2. Scroll to "Repository name" field
3. Change "PiPi" to the chosen name (from decision above)
4. Click "Rename"
5. Confirm the rename when prompted

**Note:** GitHub will automatically redirect git operations (clone, fetch, push) from the old URL to the new URL. However, GitHub Pages URLs do NOT redirect - the old URL will return 404 after rename.

After rename, the repository will be at:
- New URL: https://github.com/goom1000/{new-name}
- Old URL: https://github.com/goom1000/PiPi (redirects for git, 404 for Pages)
  </instructions>
  <resume-signal>Type "renamed to {new-name}" to continue (e.g., "renamed to cue-app")</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Update Vite base path configuration</name>
  <files>
    - vite.config.ts
  </files>
  <action>
Update the Vite configuration to use the new repository name as the base path:

1. **vite.config.ts:**
   - Line 8: Change `base: '/PiPi/'` to `base: '/{new-repo-name}/'`

   For example, if the new repo name is "cue-app":
   ```typescript
   base: '/cue-app/',
   ```

2. Also update the favicon path in index.html if it uses absolute path:
   - Check: `grep "favicon" index.html`
   - If path is `/PiPi/favicon.png`, change to `/{new-repo-name}/favicon.png`
   - If path is relative (`favicon.png` or `./favicon.png`), no change needed

The base path MUST match the GitHub repository name exactly (case-sensitive).
  </action>
  <verify>
1. Verify vite.config.ts has correct base path:
   `grep "base:" vite.config.ts`
2. Build the project: `npm run build`
3. Check built index.html has correct asset paths:
   `grep -o 'src="[^"]*"' dist/index.html | head -5`
   `grep -o 'href="[^"]*"' dist/index.html | head -5`
   All paths should start with `/{new-repo-name}/`
  </verify>
  <done>Vite base path updated to match new repository name, build succeeds with correct asset paths.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to GitHub Pages</name>
  <files>None (deployment)</files>
  <action>
Commit the configuration changes and push to trigger GitHub Actions deployment:

1. Stage changes:
   ```bash
   git add vite.config.ts index.html
   ```

2. Commit with descriptive message:
   ```bash
   git commit -m "deploy: update base path for repository rename to {new-name}"
   ```

3. Push to main branch to trigger deployment:
   ```bash
   git push origin main
   ```

4. Monitor GitHub Actions:
   - Go to: https://github.com/goom1000/{new-name}/actions
   - Wait for the deployment workflow to complete (usually ~1-2 minutes)

5. Verify deployment:
   - Visit: https://goom1000.github.io/{new-name}/
   - Check browser console for any 404 errors
   - Verify all assets load (no broken images, styles applied)
  </action>
  <verify>
1. `git log -1 --oneline` shows the deployment commit
2. GitHub Actions shows successful deployment
3. Visit https://goom1000.github.io/{new-name}/ - app loads
4. Browser console shows no 404 errors
5. App is fully functional (can upload PDF, generate slides, save/load files)
  </verify>
  <done>Application deployed to new GitHub Pages URL, all assets loading correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete rebrand deployment - app accessible at new URL with all "Cue" branding</what-built>
  <how-to-verify>
1. Visit: https://goom1000.github.io/{new-name}/
2. Verify browser tab shows "Cue"
3. Verify header shows "Cue"
4. Verify landing page shows "Cue"
5. Upload a PDF and generate slides - verify AI prompts work
6. Save presentation - verify it downloads as .cue file
7. Load an existing .pipi file (if you have one) - verify backward compatibility
8. Check browser console (F12) for any errors
9. Test full presentation flow: create slides -> present -> student view

**Expected:** All "Cue" branding visible, no "PiPi" references, full functionality preserved.
  </how-to-verify>
  <resume-signal>Type "verified" if everything works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Repository renamed on GitHub
2. Vite base path matches new repository name
3. GitHub Actions deployment succeeded
4. App accessible at new URL: https://goom1000.github.io/{new-name}/
5. All assets load (no 404s in console)
6. Full rebrand visible (Cue everywhere, no PiPi)
7. All functionality preserved
</verification>

<success_criteria>
- REPO-01: GitHub repository renamed to professional name
- REPO-02: GitHub Pages URL reflects new name
- Application fully functional at new URL
- No broken assets or 404 errors
- Complete v2.5 rebrand shipped
</success_criteria>

<output>
After completion, create `.planning/phases/19-rebrand-to-cue/19-02-SUMMARY.md` documenting:
- New repository name chosen
- New GitHub Pages URL
- Deployment verification results
- Any issues encountered during rename/deployment
</output>
